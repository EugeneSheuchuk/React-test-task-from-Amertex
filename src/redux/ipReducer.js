const initialState = {};

const ON_CHANGE_FIELD = 'socialNetwork/auth/ON_CHANGE_FIELD';

const ipReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};


export default ipReducer;